{
  "name": "dsh-patch-scripts",
  "version": "0.1.5-rc.2-2",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "patch": "node src/cli.mjs",
    "doctor": "node src/cli.mjs doctor",
    "test": "node --test tests/*.test.mjs",
    "test:official": "node src/cli.mjs test --deployment official-0.1.5-rc.2",
    "check": "node --check src/*.mjs && node --check patches/*.mjs && node --check deployments/*.mjs && node --check tests/*.mjs && node --test tests/*.test.mjs && node src/cli.mjs test --deployment official-0.1.5-rc.2"
  },
  "dependencies": {
    "yaml": "2.8.2"
  }
}
